export enum OpponentWeapon {
  Rock = 'A',
  Paper = 'B',
  Scissors = 'C',
}

export enum YourWeapon {
  Rock = 'X',
  Paper = 'Y',
  Scissors = 'Z',
}

export enum WeaponScore {
  Rock = 1,
  Paper = 2,
  Scissors = 3,
}

export enum ResultScore {
  lost = 0,
  draw = 3,
  won = 6,
}

export enum MatchResult {
  lost = 'X',
  draw = 'Y',
  won = 'Z',
}
